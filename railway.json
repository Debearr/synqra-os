{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "cd noid-dashboard && npm install",
    "buildCommand": "cd noid-dashboard && npm run build"
  },
  "deploy": {
    "startCommand": "cd noid-dashboard && npm run start",
    "healthcheckPath": "/",
    "restartPolicyType": "ALWAYS"
  }
}
