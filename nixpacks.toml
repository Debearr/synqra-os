[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm --prefix apps/synqra-mvp install"
]

[phases.build]
cmds = [
  "npm --prefix apps/synqra-mvp run build"
]

[phases.start]
cmd = "npm --prefix apps/synqra-mvp start"
