[phases.setup]
nixPkgs = ['nodejs-20_x', 'pnpm-9_x']

[phases.install]
cmds = ['cd apps/synqra-mvp && pnpm install --frozen-lockfile']

[phases.build]
cmds = ['cd apps/synqra-mvp && pnpm run build']

[start]
cmd = 'cd apps/synqra-mvp && pnpm start'
