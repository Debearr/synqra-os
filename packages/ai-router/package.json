{
  "name": "@noid/ai-router",
  "version": "1.0.0",
  "private": true,
  "description": "AI model routing and orchestration with 80% local / 20% external split",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo 'Lint not configured yet'",
    "test": "echo 'Tests not configured yet'"
  },
  "dependencies": {
    "@noid/database": "workspace:*",
    "@anthropic-ai/sdk": "^0.68.0",
    "@huggingface/inference": "^2.8.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@noid/tsconfig": "workspace:*",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
